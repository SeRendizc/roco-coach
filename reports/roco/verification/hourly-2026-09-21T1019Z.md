# 小芽进度（小时报，2026-09-21 10:19Z）

覆盖时段：约 09:19Z — 10:19Z。**这一段是 P0 收口 + 可移植化合并，已提交并推送。**

## 一、这一小时完成的事（都有落盘证据）

| # | 完成项 | 证据 | 状态 |
|---|---|---|---|
| 1 | **P0 关掉**：模型臂 **108 条**规则回执重录（`by_kind={receipt-digest-mismatch:108}`，只有 `query_rules` 这一类） | `2b9fa12`；`verify-agent-trajectories --trajectories tests/evals/agent-trajectories-model-v1.jsonl --quiet` → rc=0；模型臂 1752 行、错误轨迹 135 条只有头行 `source_sha256` 跟着改 | ✅ 已提交 |
| 2 | **14 套件门禁串行全绿**（第一次跑是红的，见第二节） | `node scripts/roco/verify-release.mjs` → verdict=**pass**、rc=0；`latest.json` 14/14、`last-green.json` head `cfe7f63`（12:15 那份 `9811f548` 是旧的） | ✅ 双灯绿 |
| 3 | **小芽可移植化**：显式游戏适配契约 + mock 宿主夹具 | `eb34be9`；`src/coach/game-adapter.js` + `docs/roco/GAME-ADAPTER.md`；`npm run test:mock-host` **17/17**（8 对局 + 3 非对局场景，双方阵容覆盖 **48/48**，不依赖演示页 DOM） | ✅ 已合并 |
| 4 | 六条硬行为逐条有数字 + 必红反证 | 检测器 P50 **0.021ms**/P95 **0.049ms**；陈旧结果取消（`state-advanced`/`after-deadline`，反证：版本没变必须接受）；总时限 **3000ms**（正常 P50 **64ms**，挂起 **3002ms** 回退规则短提示）；未核验机制 fail closed（引擎明确拒绝率 **1.0**，对照探针成功）；`pvp-live` 静默（PvE 对照必须能开口）；规则引擎压过 LLM | ✅ 有判据 |
| 5 | 浏览器可见行为 + 负载证据 | `reports/roco/adapter-acceptance/`：13/13 + 6 张截图，`clientW == scrollW`（1440/1440、390/390），真实键鼠命中 `#auto-turn`；`reports/roco/adapter-load/adapter-load.json` | ✅ 已提交 |
| 6 | 顺带修掉两处**静默失效**（都不报错，只是从来没生效） | `intervention-model.js` 在浏览器抛 `ReferenceError` 被吞成 `layer-error` → 判定层**从来没跑过**；`rocoGameView` 把模式写死成 `pve` → PVP 门控永不命中 | ✅ 已修 |
| 7 | 进度台账 + 状态文档 | `831c3c5`（A65-1…19 进 `PROGRESS.md`；`DSH-EXECUTION-STATE.md` 声明 HEAD 更新、两条新教训）；`git push`：`4712cbf..831c3c5` | ✅ 已推送 |

## 二、门禁抓到一次**真实回退**（这一小时最有价值的一件事）

可移植化把「比较区」从页面搬进核心（`src/coach/compare-model.js`）时，核心那行把
`power_status: 'not_provided_by_source'` 直译成 **「威力：来源未给」** —— 第 64 轮明令
不许出现在玩家层的工程话，**从核心里回到了玩家点得到的比较区**。

- 判据：`demo-acceptance` ④（正则 `/来源未给|not_provided_by_source|power_status/`）
- 实际报错原文：`✖ ④ 展开的比较区里也没有工程话（威力没给就是不写，不写「来源未给」） — 命中 来源未给`
- 修法：玩家层**没给就不写**；事实不丢 —— 原始 `power_status` 落进同一行的 `dev` 字段与开发者抽屉。
- 复跑：`demo-acceptance` **116 通过 / 0 失败**，随后 14/14 全绿。

**教训**：重构会把纪律从一处搬到另一处，而判据只覆盖它原来在的地方；这一条只有浏览器层的产品判据抓得到，单测全绿证明不了它。

## 三、正在做（下一个小时）

| # | 在做 | 判据 |
|---|---|---|
| A | 精灵/技能级 **RAG 出处**（`evidence_ids`）从引擎 → Node 映射层 → mock 宿主**一路打通**，并把现在那条「如实探测」的弱判据升级成真判据（配反证） | `A65-16` 从 `PARTIAL` 升级；做不到就**不许**改状态 |
| B | 给「Coach 核心不读 DOM / 不 import `src/client/**`」装上**有牙**的结构判据，并在 `guard-selftest` 登记注入 | 两条注入必须让新判据变红；不许出现 `still_green`（空的守卫） |

两个子任务并行，各自**不许**跑门禁、**不许** commit；合并后由主线程串行跑一次 gate 再提交。

## 四、阻塞（三个外部阻塞，一个都没变）

1. **一个 DeepSeek key** —— 云臂对照跑不了（没有它就不声称本地模型可替代云端）。
2. **3—5 个真人** —— W5-05 盲评（程序合法率不等于质量）。
3. **一次真机实测 + 录屏** —— E03/F03。

**明确延后、由用户自行恢复**：人工评测、Qwen 27B 的部署/微调
（指南：`docs/roco/QWEN-27B-USER-RUN-GUIDE.md`；台账记 `A65-19 NEEDS_HUMAN`）。

## 五、如实登记的缺口（没修成绿的）

- **RL 判定层在页面上仍是 `off`**：`layer-error` 已修且有反证，但默认档位不改任何结论；`on` 档需要真人审阅，**未获准** —— 不声称它生效。
- 精灵/技能级 `evidence_ids` 在 `src/server/roco-service.js` 的 roster 映射层被丢掉（本报告第三节 A 正在修）。
- 负载数字来自**本机 Node + 本机 Python 子进程**，不是手游真机端到端；超时率是**构造**出来的。
- 已知竞态（未改）：`/api/roco/plan` 与 `/api/roco/battle/advance` 并发时，plan 回执带的是它开始时那一版。

## 六、ETA

子任务预计 **30—60 分钟**出报告；合并 + 串行门禁（约 5 分钟）+ 提交 ≈ **下一个小时内**完成并推送。
