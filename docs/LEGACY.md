# 版本与留档说明

## 两句话

- **v0.1 = 已经做完的那个小芽 Coach**（2026-09-18 完成），
  一条命令就能跑起来：`./run-v0.1.sh`
- **M0/M1 数据层是它之上的开发工作**，还没接管 UI，所以**游戏部分与 v0.1 完全一致**。

---

## 怎么看这个仓库的版本

| tag | 是什么 | 可玩 | GitHub Release |
|---|---|---|---|
| **`v0.1.0`** | 第一版**完成品**：自创宠物 Demo + AI Coach「小芽」 | ✅ 可玩 | ✅ 有（Latest） |
| `milestone-m0m1-data` | **内部里程碑**：手游数据地基（M0/M1），未完成品 | ❌ 不改变玩法 | ❌ 没有（故意的） |

两个 tag 的区别在于**能不能交付**：
v0.1 有 375 项测试全通过、能完整游玩、三种角色都是可运行闭环；
M0/M1 只有数据与文档，12 只手游精灵全部停在 `KNOWLEDGE_ONLY`，不能进入战斗。
所以只有 v0.1 是一个 Release，M0/M1 只打 tag 留个坐标。

---

## 跑 v0.1

```sh
git clone https://github.com/SeRendizc/roco-coach.git
cd roco-coach
./run-v0.1.sh
```

它会自动取出一份 v0.1 检出、挑一个空闲端口、起服务、打开浏览器。
不需要 Python，不需要下载模型，不需要 API Key。

```sh
./run-v0.1.sh --port 9000   # 指定端口
./run-v0.1.sh --no-open     # 不自动打开浏览器
./run-v0.1.sh --stop        # 停掉它启动的服务
./run-v0.1.sh --clean       # 移除 v0.1 检出
```

如果 8765 上已经有一个小芽服务在跑，脚本会**直接复用**它，不会去抢端口。

### 为什么需要这个脚本

v0.1 的代码在 git 历史里（M0/M1 的提交在它**之上**），
所以「跑 v0.1」= 把那个 tag 取出来跑。

脚本用 `git worktree` 在仓库旁边放一份检出到 `.v0.1-run/`（已加入 `.gitignore`）：

- **不污染当前工作区**，不切换你的分支，不影响 `master` 的代码
- 删掉也简单：`./run-v0.1.sh --clean`

手动跑也一样：

```sh
git worktree add ../roco-v0.1 v0.1.0
cd ../roco-v0.1 && npm start        # http://127.0.0.1:8765/
```

---

## 关于「旧代码几个 G」

**网络上这个仓库只有 46 MB，`.git` 目录 65 MB，没有几个 G。**

「几个 G」是**本机工作目录**里那些被 `.gitignore` 忽略的东西：

| 目录 | 大小 | 说明 |
|---|---|---|
| `tmp/` | 2.7 G | 开发过程中的截图、Chrome 临时 profile、调试产物 |
| `.models/` | 723 M | 语义检索与 SmolLM2 实验下载的模型权重 |
| `.venv-agent/` | 641 M | Python 虚拟环境 |

这三样**本来就不在版本控制里**，换台机器克隆不会带上它们，也不影响运行
（缺了会自动退回词项检索，不报错中断）。

所以「删掉旧代码」在这里既没有必要也没有收益：

- 真正的旧版本本来就只以**压缩后的 git 历史**形式存在，只占几十 MB
- 删掉它反而会让「随时能跑起 v0.1」这件事消失

用 tag 而不是复制一份代码到 `legacy/` 子目录，是为了让代码只有一条历史、
只有一个真相来源，避免以后修 bug 要改两遍、测试也要跑两遍。

---

## v0.1 是什么

| 项 | 值 |
|---|---|
| tag | `v0.1.0` |
| commit | `1717cd515e8d900e6f2ccccb8707a0a85a809989`（2026-09-18） |
| 界面版本 | 营地 `v0.11` |
| 引擎版本 | `RULES_VERSION = 0.6` |
| 自创宠物 | 14 只（烬尾狐、潮甲龟、芽角鹿、炽鬃狮、溪刃獭、苔盾菇、砾背獾、鸣电雀、岚翎隼、云绒蛾、晶角犀、伏光貂、磐耳羊、灵瞳猫） |
| 自创技能 | 23 条 |
| 属性 | 7 种，两个独立三环相克（火→草→水→火、岩→雷→风→岩） |
| 机制 | 6 选 4 配招、3 件携带物、2 种环境、强化/驱散、灼烧/中毒/减速、补位与换宠 |
| 测试 | 375 项全通过（357 unit + 18 browser） |
| 启动 | `./run-v0.1.sh` 或 `npm start`（8765） |

三种角色都做成了可运行的闭环：**军师**（局内一句话 + 可展开依据）、
**老师**（复盘与阶段小测）、**陪练**（独立气泡、有情绪、记得偏好）。

---

## M0/M1 做了什么（以及为什么没动运行代码）

M0/M1 只建立**可信的数据地基**：

| 产物 | 入口 |
|---|---|
| 来源与许可台账（3 份快照按 revision + SHA256 冻结） | `data/roco/sources.yaml`、`docs/roco/LICENSE-MATRIX.md` |
| 规范化数据（12 只精灵 / 824 技能 / 312 学习表 / 120 属性组合） | `data/roco/normalized/` |
| 仓库审计（逐模块 KEEP / ADAPT / RETIRE / MISSING） | `docs/roco/M0-REPO-AUDIT.md` |
| 冲突记录（26 处差异全部分类，未解决 0） | `docs/roco/DATA-CONFLICTS.md` |
| 支持矩阵 | `docs/roco/PET-SUPPORT-MATRIX.md` |
| microcase 计划（21 条） | `docs/roco/MICROCASE-PLAN.md` |
| 验收 checklist | `docs/roco/M0-M1-ACCEPTANCE.md` |

**为什么不动引擎**：手游的官方伤害公式、等级换算、同速裁决、能量上限、
印记替换规则、百分比取整方向、A 组 6 只特性——这 7 项在公开资料里都找不到
一手依据。在这种情况下改引擎，等于用自创规则冒充手游机制，正是交接材料禁止的。

所以这 7 项被转成了 **21 条 microcase 与 74 个未解问题**，
**没有用任何默认值补齐**。下一轮（M2）才新建 Python `roco_env` 作为手游规则唯一来源。

在那之前，A/B/C 三组 12 只精灵**都不能进入战斗**。

---

## 相关文档

- `docs/RELEASE-v0.1.md` — v0.1 发行说明
- `docs/roco/M0-REPO-AUDIT.md` — 旧版每个模块的去向
- `docs/roco/M0-M1-ACCEPTANCE.md` — M0/M1 逐项验收与证据
- `docs/roadmap/DSH-EXECUTION-STATE.md` — 断点状态，接手时先读这个
