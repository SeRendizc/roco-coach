# 外部诊断与领域调研：采纳记录

输入：用户提供的一份外部诊断与领域调研资料（原文件为 `docs/PROJECT-DIAGNOSIS.md`、`docs/DOMAIN-RESEARCH.md` 与 `docs/sources/verified-sources-*.md` 共 4 份）。它们是研究/诊断资料，不是新的执行授权；没有照文档指令删清单、初始化提交或宣称所有任务完成。

> 溯源说明：上述 4 个文件在后来替换项目目录时未被保留，**当前仓库内已不存在**，因此无法逐条复核原文。本记录仅保留当时的判断与取舍，属于自述，不是可核验引用。后续如再引用其中的结论，应重新生成资料而不是引用本记录。

## 优先采纳并修复

1. 运行态落后：确认 8765 原 PID 12893 的工作目录属于当前项目，重启后 bootstrap 提供 runtimeVersion=0.8（**已过时**：`src/server/index.js:69` 现在是 `'0.11'`）。内存密钥需重新连接，成长存档仍保留。今后验收必须同时记录 UI 和服务端版本。
2. 模板截流：整局复盘和回合解释不再由客户端强制返回本地文本。服务器给模型提供规则分析、公开局面、最新事件、历史对话；模型解释失败才回退，并如实显示原因。小测判题/设置确认/精确规则查询保持确定性。
3. 用户截图回归：免费补位下“damn咋办”应读取当前队伍，结束后“分析”应指向整局。已增加单元测试和真实浏览器验收。
4. 自动介入：结算立即展示本局的一个改进点与可展开证据；已连接时自动请求一次模型整局分析，静默不请求。~~不再叠加无信息的首次倒下安慰气泡。~~（**2026-09-17 晚更正：首次倒下的气泡现在存在且已接线**——`src/client/app.js` 的 `notify(ev)` 调 `coachEvent(...)`，模板在 `src/coach/companion.js`；它改成了引用真实对手、倒下顺序与首个减员回合，而不是空泛安慰。所以这条当时指的是"不再叠加旧版通用安慰"，不是"没有这个通道"。）
5. 过期文档：docs/DEEPSEEK.md 修正自动调用说明；docs/coach-design-notes.md 明确标历史稿；README 更正运行版本；测试报告重新生成。

## 采纳到后续验收，不代替眼前修复

- 工具选择评测分为需工具、不需工具、跨工具补证据、应停止；独立评价选择质量和调用数量。
- RAG 评测分开检索命中、回答忠实性、行动合法性；增加口语、反例、应沉默场景。不能仅用知识卡数量证明价值。
- 学习评价分开提示后做对与后续无提示做对；自测只展示机制，不证明玩家学习增益。
- 新来源涉及教学帮助强度和专家反转效应，适合支撑研究动机；效应量与实验结论引用前仍需独立核实。

## 不直接采用的推论

- 不把原来未完成的几十项移到“本期不做”来得到零未完成；保留用户要求及原始勾选依据。
- 关键词与概念扩展在14条正例中相差1条，只说明本测试没观察到提升，不能推成RAG无效或永远无法完成。它也不是BM25与神经向量检索的直接比较。
- Q表实验的目标含打扰成本；只拿帮助次数评价胜负并不完整。但它确实没训练LLM权重、没接入真人产品，必须明确。
- 各论文在不同设置下的胜率不能排列成普遍的“RL > LLM > 启发式”，也不能证明某架构永远最优。
- 未检索到直接竞品不等于领域空白或首创；不采用“这个位置是空的”的面试话术。
- 新增30–50条真实模型用例可形成有限验收，不代表自动证明泛化能力；调用成本需要按实际用量核算，不能预先称可忽略。

## 本轮未完成

v0.8真实DeepSeek复盘质量仍待重新录入密钥后验证；现有模型路由测试使用受控替身，不能冒充真实生成。（**2026-09-17 晚更正：其后已测**——`reports/live-model-v08.json`、`reports/live-model-v10.json`、以及 44 条预注册用例三轮共 132 次真实调用 `reports/live-model-eval.json`。这句话仍成立的边界是：每次评测都标注了自己的样本与置信区间局限。）外部资料的“已核验”是原作者的声明，本轮未逐篇重新核验所有论文。
