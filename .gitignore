.venv-agent/
.models/
__pycache__/
*.pyc
.DS_Store
tmp/

# ── M1 上游原始快照 ────────────────────────────────────────────────
# 解压后的第三方快照体积大且属他人版权，不入库。
# 归档是否入库按**体积**分开处理，避免把 177MB 的 NRC_AI 归档塞进 git：
#   data/roco/raw/rocom-wiki-data.tar.gz  (844KB)  ← 入库（主数据源，M1 的核心证据）
#   data/roco/raw/rocom-data.tar.gz       (4.0MB)  ← 入库（第三来源）
#   data/roco/raw/NRC_AI.tar.gz           (172MB)  ← **不入库**，见下方说明
#
# NRC_AI 归档不入库的理由与替代保证：
#   1. 它在 git 里会被完整保留在历史中，之后无法真正移除，代价远大于收益；
#   2. 它的角色只是**交叉核验的第二来源**，其数值不进入 normalized/（许可未确认，
#      标 REFERENCE_ONLY），所以丢失它不会让任何已交付结论失去依据；
#   3. 可复现性由「固定 revision + 归档 SHA256 + 解压后逐文件 SHA256」保证：
#        revision          → data/roco/sources.yaml（nrc-ai-sqlite）
#        归档 SHA256        → reports/roco/m1-data/raw-sha256.txt
#        逐文件 SHA256      → reports/roco/m1-data/snapshot-file-inventory.json
#        交叉核验结果       → reports/roco/m1-data/cross-check.json
#     任何人可用 sources.yaml 里的 archive_url 重新下载并核对上述哈希。
data/roco/raw/extracted/
data/roco/raw/NRC_AI.tar.gz
.dsh-pushplus-token
reports/roco/notify-log.jsonl

# W3-03 轨迹：**只入库 manifest**（规模、切分、口径、种子）。
# 12,000 局产生约 91MB 的 jsonl；把它们塞进 git 会让仓库无法维护，
# 而它们**完全可复现**：manifest 里有 base_seed / split_seed / turn_limit / 策略版本，
# 跑 `python3 scripts/roco/build-trajectories.py` 就能逐字节重放。
reports/roco/trajectories/*.jsonl

# 浏览器验收：结论入库，**每次运行都变的那一份**与截图不入库。
# 截图每次都是新文件（图片字节不同），留在库里会让 git 每次都被「改了」淹没。
reports/roco/acceptance/*-run.json
reports/roco/acceptance/*.png
reports/roco/demo-acceptance/*-run.json
reports/roco/microcases/*-run.json
reports/roco/dashboard-run.json
# 通用：所有报告的易变运行信息都叫 *-run.json
reports/roco/*-run.json
reports/roco/demo-acceptance/*.png

# v0.1 一键启动产生的检出
.v0.1-run/

# 本地推理虚拟环境与下载缓存（第 14 轮）：权重与依赖都不入库
.venv-mlx/
tmp/uv-cache/

# 本地模型运行产物（日志、PID、请求记录）：每次跑都变，只留 bench.json 当证据
reports/roco/local-model/*.log
reports/roco/local-model/*.pid
reports/roco/local-model/responses.jsonl
reports/roco/local-model/requests.fifo
