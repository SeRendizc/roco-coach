# 旧版本（自创宠物 Demo）留档说明

## 一句话

**旧版本没有被删除。** 它就在这个仓库的 git 历史里，用一个 tag 钉住，
随时可以取回、运行、对比。

```sh
git tag -l                       # 看到 legacy-demo-v0.11
git worktree add ../roco-legacy legacy-demo-v0.11   # 在旁边的目录取出一份旧版
cd ../roco-legacy && npm start   # 直接启动旧版，端口仍是 8765
```

跑完删掉工作树即可，不影响主目录：

```sh
git worktree remove ../roco-legacy
```

也可以只取某一个文件，而不取整个旧版：

```sh
git show legacy-demo-v0.11:engine.js > /tmp/old-engine.js
```

---

## 为什么用 tag 而不是「另存一份」

| 方案 | 问题 |
|---|---|
| 复制一份到 `legacy/` 子目录 | 同一批代码出现两份，之后修 bug 要改两遍；测试也会重复跑 |
| 开一个 `legacy` 分支 | 能表达「冻结」，但看代码要切分支，容易忘记自己在哪一版 |
| **打 tag** | 代码只有一份、只有一条历史；旧版是一个**不可变的坐标**，随时可查、可跑、可 diff |

git 历史本身就是留档机制。这个仓库的历史只有 4 天、130 个提交、
`git` 目录 **65 MB**，没有「几个 G」——那几个 G 是本地被忽略的
`tmp/`、`.models/`、`.venv-agent/`，它们本来就不在版本控制里，也不影响克隆。

所以「删掉旧代码」在这里既没有必要，也没有收益：真正的旧版本本来就
只以压缩后的历史形式存在。为了可读性，本文件把它的坐标和取用方式写清楚。

---

## 旧版本是什么

| 项 | 值 |
|---|---|
| tag | `legacy-demo-v0.11` |
| 对应 commit | `1717cd515e8d900e6f2ccccb8707a0a85a809989` |
| 版本号 | 界面显示 `营地 · v0.11`，引擎 `RULES_VERSION=0.6` |
| 形态 | 自创宠物对战游戏 + 内嵌 AI Coach「小芽」 |
| 自创宠物 | 14 只（烬尾狐、潮甲龟、芽角鹿、炽鬃狮、溪刃獭、苔盾菇、砾背獾、鸣电雀、岚翎隼、云绒蛾、晶角犀、伏光貂、磐耳羊、灵瞳猫） |
| 自创技能 | 23 条 |
| 属性 | 7 种（火、水、草、普通、岩、雷、风），两个三环相克 |
| 测试 | 375 项全通过（357 unit + 18 browser） |
| 启动 | `npm start` → http://127.0.0.1:8765/ |

**它与《洛克王国：世界》手游没有数据关系。** 它的价值在于证明产品交互：
主动介入时机、有界只读工具循环、事实校验、回放、评测底座。
这些底座被判定为 KEEP，会在手游规则域上继续复用（见 `docs/roco/M0-REPO-AUDIT.md`）。

---

## 当前版本与旧版本的关系

| | 旧版本 `legacy-demo-v0.11` | 当前 `master` |
|---|---|---|
| 玩什么 | 自创宠物 Demo | **还是自创宠物 Demo** |
| `npm start` | 8765，可玩 | 8765，可玩（一模一样） |
| 手游数据 | 无 | `data/roco/**`（12 只精灵的静态数据） |
| 手游规则引擎 | 无 | **仍然没有**（下一轮 M2 才做） |
| 默认 UI | 自创宠物 | **未改动** |
| 测试 | 375 | 390（+15 数据域验收） |

**关键点：`master` 上的游戏部分与旧版完全一致。**
M0/M1 只增加了数据与文档，没有接管 UI、没有替换宠物、没有重写引擎。
所以你现在想启动「原始项目」，直接在主目录 `npm start` 就是那个熟悉的版本。

---

## 本轮（M0/M1）为什么刻意不动运行代码

实施书把 M0/M1 的边界写得很清楚：这一轮只建立**可信的数据地基**，
不提前替换 UI、不重写战斗引擎。理由不是保守，而是顺序：

1. **手游规则还没有证据。** 官方伤害公式、等级换算、同速裁决、能量上限、
   印记替换规则、百分比取整方向——这 7 项在公开资料里都找不到一手依据。
   在这种情况下改引擎，等于用自创规则冒充手游机制，正是交接材料禁止的。
2. **先把问题问准。** 本轮把这 7 项缺口转成了 21 条 microcase
   （`docs/roco/MICROCASE-PLAN.md`），74 个未解问题逐条登记，
   **没有用任何默认值补齐**。
3. **旧版是回归网。** 375 项测试跑的是旧引擎；只要它们还全绿，
   就证明新加的数据层没有意外影响运行态。

下一轮（M2）才会新建 Python `roco_env` 作为手游规则唯一来源，
并逐步把 A 组 6 只从 `KNOWLEDGE_ONLY` 升到 `SIM_PARTIAL`、再到 `SIM_VERIFIED`。
在那之前，A/B/C 三组都**不能进入战斗**。

---

## 相关文档

- `docs/roco/M0-REPO-AUDIT.md` — 旧版每个模块的去向（KEEP / ADAPT / RETIRE / MISSING）
- `docs/roco/M0-M1-ACCEPTANCE.md` — 本轮逐项验收与证据
- `docs/roco/PET-SUPPORT-MATRIX.md` — 12 只手游精灵的支持矩阵
- `docs/roadmap/DSH-EXECUTION-STATE.md` — 断点状态，接手时先读这个
